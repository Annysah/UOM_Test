<div">
    <h1>People</h1>
    {% for post in posts %}
        <div>
            <img src="{{ post.thumbnail.src }}" alt="{{ post.title }}">
            <h2>{{ post.title }}</h2>
            <p>{{ post.excerpt }}</p>
        </div>
    {% endfor %}
</div>
